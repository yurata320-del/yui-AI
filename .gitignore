# 依存パッケージ(npm install でいつでも入れ直せるので、GitHubには上げない)
/node_modules

# Next.jsが自動で作るファイル
/.next
/out
next-env.d.ts
*.tsbuildinfo

# パスワードやAPIキーを入れるファイル(ぜったいにGitHubに上げない)
.env
.env.local
.env*.local

# Mac が勝手に作るファイル
.DS_Store

# ログ
npm-debug.log*
yarn-error.log*
