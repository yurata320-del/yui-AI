# CLAUDE.md

このリポジトリは **プログラミングを学びはじめた子どもが、AIといっしょにアプリを作るための練習場** です。
Claude Code はこのファイルのルールを必ず守ってください。

## 開発者について

現在の開発者は**保護者（大人）**です。作ったアプリを使うのは子どもです。
（2026-09-05 に子どもから引き継ぎ。子どもが再び開発する場合は、このセクションを戻してください）

## 話しかたのルール

1. **通常の技術的な日本語でよい。** 専門用語（state / props / SSG / CORS など）はそのまま使う。
2. **簡潔に。** 前置きや同じ説明の繰り返しをしない。結論から書く。
3. **判断が必要な点は明示する。** コスト・セキュリティ・後戻りできない操作は、実行前に必ず伝える。
4. **できないこと・未検証のことは正直に書く。** 「動くはず」で終わらせない。

ただし **コード内の日本語コメントは、やさしい表現のまま残す**こと（子どもが読むため）。

## コードを書くときのルール

1. **1回の変更は小さく。** いちどに何ファイルも書きかえない。1つのアプリ・1つの機能ずつ。
2. **コードには日本語のコメントを入れる。** なぜそう書いたかを、あとから読んで分かるように。
3. **むずかしい書きかたを使わない。** 子どもがコードを読む前提。短く賢く書くより、読んで分かる書きかたを選ぶ。
   - 使ってよい: `useState` / `useEffect` / `map` / `filter` / `if` / 関数
   - 相談なしに持ち込まない: 外部ライブラリの追加、状態管理ライブラリ、複雑な型パズル、正規表現の多用
4. **新しいライブラリを入れる前に、必ず理由を説明して確認を取る。** `npm install` を勝手に実行しない。
5. **既存のアプリ（counter / janken / memo）は、本人が頼まないかぎり書きかえない。** お手本として残しておく。

## デザインのルール

1. **色や大きさを自分で決めず、`app/globals.css` の `ui-◯◯` クラスを使う。**
   （`ui-card` / `ui-page-title` / `ui-btn-primary` など。一覧は README.md）
2. 足りないときだけ `globals.css` に新しく足す。そのときは何を足したか説明する。
3. タイルの色は README にある5色から選ぶ。

## 新しいアプリを作るときの手順

必ずこの順番で、1ステップずつ確認しながら進める:

1. `app/apps/<なまえ>/page.tsx` を作る
2. ブラウザで見られることを確認してもらう
3. `app/page.tsx` の `TILES` にタイルを1つ足す
4. ホームから開けることを確認してもらう

## 安全のルール

1. **個人情報をコードに書かない。** 本名・住所・学校名・電話番号・写真など。
   本人が書こうとしたら、理由を説明してやさしく止める。
2. **パスワードやAPIキーはコードに書かない。** `.env.local` に置く。
3. **`git push` や公開（デプロイ）は、本人が明確に頼んだときだけ。** 勝手に外に出さない。
4. **ファイルやフォルダを消す前に、必ず確認する。**
5. このリポジトリの外（`~/Desktop/yui-AI` の外）のファイルは、頼まれても触らない。

## 技術メモ（参考）

- Next.js 14 (App Router) / React 18 / TypeScript / Tailwind CSS v3
- データベースは**まだ入れていない**。保存が必要なアプリは、まず `localStorage`（memoアプリが見本）で作る。
  本格的なDBが必要になったら、そのとき相談して足す。
- 外部APIも使っていないので、お金はかからない。有料サービスが必要になる提案をするときは、必ず先に伝える。

## Stitch のデザインを反映するときのルール

Stitch（デザインを作るツール）で作った画面を、このアプリに実装するときは必ず守ること。

1. **必ず MCP の `get_screen_code` で本物のHTML/CSSを取ってきてから実装する。**
   デザインの画像を見た印象だけで書かない。
2. **色・余白・文字の大きさ・部品の位置は、取ってきたコードの数字をそのまま使う。**
   「だいたい同じ」で近づけない。数字が見つからないときは、勝手に決めずに本人に確認する。
3. **変えていいのは見た目だけ。** 中の動き（state の使いかた、保存の処理など）は
   デザイン反映のついでに書きかえない。触る必要があるときは先に相談する。
4. **反映したあとは、Stitch側の画面と実際の画面を見くらべる。** ちがうところがあれば必ず報告する。
5. **ちがいが見つかっても、勝手に近い数字に寄せて直さない。**
   もう一度 `get_screen_code` で正しい数字を取り直してから直す。

> なぜこのルールがあるか: 見た目を「なんとなく」で近づけると、あとから
> 「どこが本物のデザインで、どこが自分の想像か」が分からなくなるから。
